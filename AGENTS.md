## プロジェクト概要

このワークスペースは、重機の自律施工を目標としたシミュレータを Unity で開発するための環境である。

## 会話履歴

会話履歴は本ワークスペースの `conversation_history/` 配下に Markdown ファイルとして保存する。
同一セッションの会話は同一ファイルに保存し、セッションが終了した場合は新しいファイルを作成する。
保存内容は要約ではなく、ユーザーとアシスタントが会話画面で表示した内容を可能な限り全文で記録する。
ただし、ターミナルコマンドの長大な出力や内部処理ログは、ユーザーに表示された重要箇所だけを記録してよい。